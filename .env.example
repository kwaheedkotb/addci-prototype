# Vercel Postgres Database Configuration
# These environment variables are automatically added when you create a Vercel Postgres database
# Go to: Vercel Dashboard > Your Project > Storage > Create Database > Postgres

POSTGRES_PRISMA_URL="postgresql://user:password@host:5432/database?pgbouncer=true&connect_timeout=15"
POSTGRES_URL_NON_POOLED="postgresql://user:password@host:5432/database"

# The above URLs will be automatically populated by Vercel when you link a Postgres database
